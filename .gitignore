# Build output
/canopy
/dist/
*.exe
*.test
*.out

# Go
vendor/

# Local Canopy state (trust records live outside observed repos, but never commit any that leak in)
.canopy/
canopy.local.yaml

# Editors
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Agent scratch worktrees. Never part of the repository they are cut from.
.claude/
