╭──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ ▲ canopy  help                                                                                                                                                   ▄▀▀▀▀ ▄▀▀▀▄ █▄  █ ▄▀▀▀▄ █▀▀▀▄ █   █ │
│                                                                                                                                                                  █     █▄▄▄█ █ ▀▄█ █   █ █▄▄▄▀  ▀▄▀  │
│                                                                                                                                                                  ▀▄▄▄▄ █   █ █   █ ▀▄▄▄▀ █       █   │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
anywhere                                                                           review
  ?                     this screen, except while a message is half typed            j / k                 move
  ctrl+c                quit, asked twice in a conversation                          enter                 open the changes, then a file
                                                                                     tab                   cycle the queue, ranking, cost outcome and overlap
chat                                                                                 c                     commit, from the file list
  enter                 send                                                         o                     a reviewer's opinion, from the ranking
  shift+tab             plan or build, which is what the agent may change            K                     credentials
  /                     the command list, which narrows as you type                  esc                   back one level
  up / down             move the command list, or otherwise your sent history
  tab                   take the highlighted command                               a diff
  //                    send a literal prompt beginning with slash                   j / k                 scroll a line
  esc                   interrupt the turn, or clear a half written message          space                 scroll a page
  ctrl+c                stop the turn, then twice to quit                            g / G                 top and end
  ctrl+n                a new conversation, keeping this one                         esc                   back to the file list
  ctrl+d                agents, and it works with a question waiting here
  ctrl+k                credentials                                                writing a commit
  ctrl+r                compact the conversation, asked twice because it spends      ctrl+s                commit. nothing is staged until then
  ctrl+o                show tool output and diffs in full, and fold them back       esc                   cancel, throwing the message away
  pgup / pgdown         scroll the conversation, or the btw panel while it is up
  mouse drag            select conversation text, copied when you let go           worktrees
  ctrl+home / ctrl+end  the top, and back to following                               j / k                 move
  alt+enter             a line break instead of sending                              g / G                 top and end, as do home and end
  !command              run it here; its output goes with your next message          r                     refresh
  ctrl+f                find in the conversation; enter older, down newer            K                     credentials
  ctrl+y                copy the last code block, or the last reply                  esc / tab / q         agents
  ctrl+p                every command, mode, theme and file, as you type
  @                     a file in this project, which narrows as you type          the model picker, on /model
  ctrl+x ctrl+e         write the message in your editor                             j / k                 move, across credentials as well as within one
  # note                keep a note in AGENTS.md for later conversations             enter                 run this conversation on it from the next message
  enter / y             allow a tool call once, while a question is up               ctrl+k                credentials, to change what a key defaults to
  a                     allow it for the rest of the session                         any other key         back, with nothing changed
  arrows / pgup         read on with a question up, deciding nothing
  any other key         refuse it                                                  credentials
                                                                                     j / k                 move
another agent's question, above the box                                              enter                 use this one for the conversation
  enter                 approve it once, while the box is empty                      m                     set which model it talks to
  backspace             decline it, while the box is empty                           e                     rename it. the value is not asked for again
  ctrl+g                open its conversation and full request                       a / n                 add one
  a                     always is answered on that full prompt only                  d / x                 remove one
                                                                                     r                     reload the list
agents                                                                               esc / tab             back to where you were
  1 to 8                jump to that pane, and again to open it                      q                     quit
  enter                 open the selected agent, or approve its question once
  backspace             decline the selected agent's question
  j/k scroll   any other key back
